# Frontend — React + Vite

Proyecto base. Instalar dependencias y levantar el servidor de desarrollo:

```bash
npm install
npm run dev
```

La URL del API se configura en `.env` con `VITE_API_URL`.
